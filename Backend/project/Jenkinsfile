pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                checkout([
                    $class: 'GitSCM',
                    branches: [[name: '*/viet']], // Đảm bảo sử dụng nhánh 'viet'
                    userRemoteConfigs: [
                        [url: 'git@github.com:haquocviet261/PetShopServer.git']
                    ]
                ])
            }
        }
        stage('Build') {
            steps {
                dir('Backend/project') { // Sử dụng đường dẫn tương đối
                    sh './mvnw clean install'
                }
            }
        }
        stage('Deploy') {
            steps {
                sh 'java -jar Backend/project/target/project-0.0.1-SNAPSHOT.jar'
            }
        }
    }

    post {
        success {
            echo 'Build successfully!'
        }
        failure {
            echo 'Build failed!'
        }
    }
}
